# Build output and tool binaries (see Makefile LOCALBIN).
bin/
# Run output (DESIGN.md §11).
botbox-out/
# Go test artifacts.
*.test
*.out
# Worktrees that subagents create for isolated work.
.claude/worktrees/
# Build output of the spike module.
docs/spikes/cert-manager-envtest/cert-manager-envtest

# rapid writes a reproduction file beside a failing property test.
testdata/rapid/
