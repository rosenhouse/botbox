# The fixture: cert-manager signs from an Issuer, and no CRD schema can invent
# a valid issuerRef (DESIGN.md §8.1).
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: selfsigned
spec:
  selfSigned: {}
